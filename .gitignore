# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

.ipynb_checkpoints/

# ML artifacts
# pre-cropped training set (#11); as large as the raw data — never commit
data/cache_crops/
checkpoints/
*.pt
*.pth
*.ts
*.onnx
wandb/
.cache/

# Canonical deployable checkpoints — tracked despite checkpoints/ and *.pt
!checkpoints/
!checkpoints/efficientnet_b0_targeted.pt
!checkpoints/efficientnet_b0_partial_ft.pt

# split manifest (regenerate deterministically from data/raw)
data/split_manifest.json

# #17 webcam bridge: framing-measurement caches and demo/saved webcam crops are
# generated; the comparison montage is committed under docs/figures instead.
data/training_framing_records*.json
data/training_framing_stats.json
data/webcam_crops_demo/
data/webcam_crops/

# Embedding caches (regenerable, large)
data/embedding_cache/
data/*.npz

# Generated error galleries (large, regenerable from scripts)
docs/figures/error_gallery_targeted/

# OS / editor
.DS_Store
.idea/
.vscode/

# AGENTS.md — per-session context, not part of the project
AGENTS.md

# figures are generated; keep the ones we deliberately commit under docs/figures
