# ASL fingerspelling recognition — Python 3.10+
# Install with:  pip install -r requirements.txt
# (On Colab most of torch/torchvision are preinstalled; still pip-install the rest.)

# --- deep learning ---
torch>=2.1
torchvision>=0.16
timm>=0.9.12               # EfficientNet-B0, ImageNet-pretrained

# --- data / augmentation ---
numpy>=1.24
albumentations>=1.4.0     # geometric + photometric + structural augmentation
opencv-python>=4.9        # capture, resize, image IO
mediapipe>=0.10.9         # hand detection/crop + selfie segmentation

# --- demo ---
pyttsx3>=2.90             # offline text-to-speech

# --- experiment tracking / data ---
wandb>=0.16              # free experiment tracking (shared team project)
kaggle>=1.6              # dataset download CLI

# --- analysis / notebooks ---
matplotlib>=3.7
scikit-learn>=1.3        # metrics, logistic-regression sanity floor
pandas>=2.0
jupyter>=1.0

# --- export ---
onnx>=1.15
onnxruntime>=1.17

# --- tests / quality ---
pytest>=7.4
